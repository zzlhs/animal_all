# GBIF Photo Globe 前端

Vue/Vite 前端与 Sites Worker。它是工作区中的独立包，运行时使用 Node.js 22.13.1。

```bash
npm install
npm run dev
```

前端支持 `sample` 和 `api` 两种数据模式；API、PostGIS 和 PMTiles 不属于前端包。完整的工作区说明见上级目录的 [`README.md`](../README.md)。
